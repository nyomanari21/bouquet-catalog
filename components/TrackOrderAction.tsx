import { supabase } from "@/lib/supabase";

export default function TrackOrderAction() {
    
    return (
        <form action="">
            <div className="flex flex-col gap-1.5">
                {/* <label className="text-xs font-semibold text-gray-700">Kode Pesanan <span className="text-red-400">*</span></label>
                <input type="text" name="order_code" value={formData.order_code} onChange={handleInputChange} placeholder="Masukkan kode pesanan" className="w-full bg-white p-2 rounded-lg text-xs border border-gray-200 outline-none focus:border-[#e75888]" required /> */}
            </div>
        </form>
    )
}